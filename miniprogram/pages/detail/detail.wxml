<!-- 图片详情页 -->
<view class="container">
  <image class="photo" src="{{photo.imageUrl}}" mode="widthFix" bindtap="previewImage" />

  <view class="info-card">
    <view class="info-row">
      <text class="info-label">拍摄日期</text>
      <text class="info-value">{{photo.photoDate}}</text>
    </view>
    <view class="info-row">
      <text class="info-label">拍摄地点</text>
      <text class="info-value">{{photo.locationName}}</text>
    </view>
    <view class="info-row" wx:if="{{photo.description}}">
      <text class="info-label">描述</text>
      <text class="info-value">{{photo.description}}</text>
    </view>
    <view class="info-row">
      <text class="info-label">上传者</text>
      <text class="info-value">{{photo.nickname}}</text>
    </view>
  </view>
</view>
